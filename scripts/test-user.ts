/**
 * Shared credentials for the seeded development user. Kept in its own module so
 * importing them doesn't execute the seed script.
 */
export const TEST_EMAIL = "test-user@cinema-brain.local";
export const TEST_PASSWORD = "cinema-brain-test-password";
